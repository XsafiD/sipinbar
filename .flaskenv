# ============================================================
# .flaskenv — Flask CLI Configuration
# ============================================================
# Dimuat otomatis oleh Flask saat menjalankan `flask run`
# TIDAK berisi secret (untuk secret gunakan .env)
# ============================================================

FLASK_APP=app.py
FLASK_ENV=development
FLASK_DEBUG=1
